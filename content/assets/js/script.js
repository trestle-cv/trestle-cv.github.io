const toggle=document.querySelector(".nav-toggle");const nav=document.querySelector("#site-nav");if(toggle&&nav){toggle.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open));nav.classList.toggle("open",!open)});nav.addEventListener("click",event=>{if(event.target.closest("a")){toggle.setAttribute("aria-expanded","false");nav.classList.remove("open")}})}
function highlightCode(source){const escaped=source.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");return escaped.replace(/(#[^\n]*|\/\/[^\n]*)|("[^"\n]*"|'[^'\n]*'|`[^`\n]*`)|(\b\d+(?:\.\d+)?\b)|(\b(?:const|let|var|function|async|await|return|if|else|for|new|true|false|null|package|import|func|map|string|curl|GET|POST|PATCH|DELETE)\b)|(&quot;[\w-]+&quot;\s*:)/g,(match,comment,string,number,keyword,key)=>`<span class="${comment?"tok-comment":string?"tok-string":number?"tok-number":keyword?"tok-keyword":"tok-key"}">${match}</span>`)}document.querySelectorAll("pre code").forEach(code=>{code.innerHTML=highlightCode(code.textContent)});
const docsNav=document.querySelector(".docs-nav");if(docsNav){const currentPath=location.pathname.replace(/\/$/,"")||"/";let current=null;docsNav.querySelectorAll("a").forEach(link=>{const linkPath=new URL(link.href,location.href).pathname.replace(/\/$/,"")||"/";if(linkPath===currentPath)current=link});docsNav.querySelectorAll("details").forEach(section=>section.open=Boolean(current&&section.contains(current)));if(current){current.setAttribute("aria-current","page");current.classList.add("current")}}
